"""Portfolio service implementation package.

Business logic lives in the submodules; the public API is re-exported
by the sibling ``portfolio_service`` facade so existing importers are
unaffected.
"""
